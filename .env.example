# Supabase Configuration
# Get these from: https://app.supabase.com/project/YOUR_PROJECT_ID/settings/api

# Public URL - Safe to expose in client-side code
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous Key - Safe to expose in client-side code (with RLS enabled)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role Key - NEVER expose this in client-side code!
# Only use in secure server-side operations
# This key bypasses Row Level Security - keep it SECRET!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# IMPORTANT SECURITY NOTES:
# 1. Never commit .env.local to git
# 2. Always use environment variables for API keys
# 3. Service role key should ONLY be used server-side
# 4. Enable Row Level Security (RLS) on all tables
# 5. Rotate keys immediately if exposed